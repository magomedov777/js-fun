//new lesson
//count odd numbers below n
//first
oddCount = (n) => Math.floor(n / 2);

function oddCount(n) {
  return Math.floor(n / 2);
}

//parseInt
function oddCount(n) {
  return parseInt(n / 2);
}
